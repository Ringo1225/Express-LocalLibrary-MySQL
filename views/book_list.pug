extends layout

block content
  h1= title

  ul
    each book in book_list
      li
        a(href="/catalog/book/" + book.id) #{book.title}
        |  (#{book.Author.family_name}, #{book.Author.first_name})

    else
      li There are no books.
